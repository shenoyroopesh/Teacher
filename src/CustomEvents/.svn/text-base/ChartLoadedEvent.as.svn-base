package CustomEvents
{
	import flash.events.Event;
	
	public class ChartLoadedEvent extends Event
	{
		public function ChartLoadedEvent(type:String, bubbles:Boolean=false, cancelable:Boolean=false)
		{
			super(type, bubbles, cancelable);
		}
	}
}